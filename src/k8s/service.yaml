# service.yaml
apiVersion: v1
kind: Service
metadata:
  name: messenger-service
  labels:
    app: messenger
spec:
  selector:
    app: messenger
  ports:
    - name: metrics
      port: 80
      targetPort: 8080
  type: ClusterIP